name: Papers Newsletter

on:
  schedule:
    - cron: '0 11 * * 2'
    # TEMPORÁRIO: remarcado para 06/09 16:10 UTC, com mais folga. Remover depois.
    - cron: '10 16 6 9 *'
  # TEMPORÁRIO: prova que o workflow dispara sozinho, sem acionamento manual.
  # paths-ignore evita que o commit do keepalive re-dispare o workflow.
  push:
    branches: [main]
    paths-ignore:
      - '.github/last-run.txt'
  workflow_dispatch:

# Necessário para o passo de keepalive empurrar o commit de timestamp.
permissions:
  contents: write

jobs:
  send-newsletter:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run newsletter
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          EMAIL_FROM: ${{ secrets.EMAIL_FROM }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          # TEMPORÁRIO: no teste do cron manda só para o próprio remetente,
          # para não duplicar a edição na caixa dos inscritos. Remover depois.
          EMAIL_RECIPIENTS: ${{ (github.event_name == 'push' || github.event.schedule == '10 16 6 9 *') && secrets.EMAIL_FROM || secrets.EMAIL_RECIPIENTS }}
        run: python3 newsletter.py

      # O GitHub desativa workflows agendados após 60 dias sem atividade no repositório.
      # Um commit de timestamp a cada execução zera esse contador. Roda mesmo se o envio
      # falhar (`always()`), senão uma falha derrubaria o agendamento junto.
      - name: Keepalive (evita desativação do cron por inatividade)
        if: always()
        run: |
          date -u +"%Y-%m-%dT%H:%M:%SZ" > .github/last-run.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/last-run.txt
          if git diff --cached --quiet; then
            echo "Nada a commitar."
          else
            git commit -m "chore: keepalive $(date -u +%F)"
            git push
          fi
